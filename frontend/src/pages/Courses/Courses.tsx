import {useEffect,useState} from "react"; import {api} from "../../api"; import {Course} from "../../types/course"; import Nav from "../../components/navigation/Nav"; import Footer from "../../components/footer/Footer"; import CourseCard from "../../components/course/CourseCard";
export default function CoursesPage(){const [courses,setCourses]=useState<Course[]>([]); useEffect(()=>{api("/api/courses").then(setCourses).catch(console.error)},[]); return <><Nav/><main><section className="page-hero courses-page-hero"><div><p className="eyebrow">THE COMPLETE LIBRARY</p><h1>Explore all courses.</h1><p className="lead">Choose a practice that fits your goals, pace and experience.</p></div></section><section className="section courses-all"><div className="course-grid all-course-grid">{courses.map(c=><CourseCard key={c.id} c={c}/>)}</div>{!courses.length&&<div className="loading">No courses available yet.</div>}</section></main><Footer/></>}
